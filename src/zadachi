function test (a, b)
{
   if (a > b)
      return true;
   if (a < b)
      return false;

}

test(2,3)

//----------------------------

function str (s){
	
	return ('Вы ввели :' + ' ' + s);
}
srt ('строка');

//----------------------------

function func3(o) {o.checked = true}
